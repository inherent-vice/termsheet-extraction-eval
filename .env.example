# Optional provider keys for custom extractor adapters.
# The bundled benchmark uses MockExtractor and does not require any API key.
# Leave these unset unless you implement a live provider adapter locally.

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
